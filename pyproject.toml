[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "smmargins"
version = "0.1.0"
description = "Marginal effects for statsmodels"

[tool.setuptools]
py-modules = ["smmargins"]
